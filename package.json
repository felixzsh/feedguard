{
  "name": "feedguard",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development bun run src/cli/index.ts",
    "start": "NODE_ENV=production bun run src/cli/index.ts",
    "build": "bun build src/cli/index.ts --compile --outfile dist/feedguard",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "install:browser": "bunx playwright install chromium"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.10.2"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "js-yaml": "^4.1.1",
    "playwright": "^1.57.0",
    "zod": "^4.1.13"
  }
}
